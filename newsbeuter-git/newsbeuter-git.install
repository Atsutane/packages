post_install() {
cat << EOF
==> if the generation of the man pages failed have a look at
==> http://www.newsbeuter.org/doc/newsbeuter.html
EOF
}

post_upgrade() {
  post_install $1
}

op=$1
shift
$op $*
